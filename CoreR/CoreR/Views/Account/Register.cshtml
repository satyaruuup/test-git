@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>CoreR | Register</title>
    <!-- Tell the browser to be responsive to screen width -->
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
    <!-- Bootstrap 3.3.5 -->
    <link rel="stylesheet" href='@Url.Content("~/bootstrap/css/bootstrap.min.css")'>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.4.0/css/font-awesome.min.css">
    <!-- Ionicons -->
    <link rel="stylesheet" href="https://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
    <!-- Theme style -->
    <link href='@Url.Content("~/dist/css/AdminLTE.css")' rel="stylesheet" />
    <!-- iCheck -->
    <link rel="stylesheet" href='@Url.Content("~/plugins/iCheck/square/blue.css")'>

    <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
        <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
        <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
</head>
<body class="hold-transition login-page">

    <div class="register-box">
        <div class="register-logo">
            <a href="#"><b>Core</b>R</a>
        </div>

        <div class="register-box-body">
            <p class="login-box-msg">Register a new membership</p>
            <form method="post">
                <div class="has-error">
                    <label class="control-label " id="ErName" for="inputError"></label>
                </div>
                <div class="form-group has-feedback">
                    <input type="text" class="form-control" id="Name" placeholder="Full name">
                    <span class="glyphicon glyphicon-user form-control-feedback"></span>
                </div>
                <div class="has-error">
                    <label class="control-label " id="ErEmail" for="inputError"></label>
                </div>
                <div class="form-group has-feedback">
                    <input type="email" class="form-control" id="Email" placeholder="Email">
                    <span class="glyphicon glyphicon-envelope form-control-feedback"></span>
                </div>
                <div class="has-error">
                    <label class="control-label " id="ErPassword" for="inputError"></label>
                </div>
                <div class="form-group has-feedback">
                    <input type="password" class="form-control" id="Password" placeholder="Password">
                    <span class="glyphicon glyphicon-lock form-control-feedback"></span>
                </div>
                <div class="has-error">
                    <label class="control-label " id="ErRePassword" for="inputError"></label>
                </div>
                <div class="form-group has-feedback">
                    <input type="password" class="form-control" id="RePassword" placeholder="Retype password">
                    <span class="glyphicon glyphicon-log-in form-control-feedback"></span>
                </div>
                <div class="form-group" id="ErMsg">

                </div>
                <div class="row">
                    <div class="col-xs-8">
                        <div class="checkbox icheck">
                            <label>
                                <input type="checkbox">
                                I agree to the <a href="#">terms</a>
                            </label>
                        </div>
                    </div>
                    <!-- /.col -->
                    <div class="col-xs-4">
                        <button type="submit" id="btnRegister" class="btn btn-primary btn-block btn-flat">Register</button>
                    </div>
                    <!-- /.col -->
                </div>
            </form>
            <a href="/Account/Login" class="text-center">I already have a membership</a>
        </div>
        <!-- /.form-box -->
    </div>
    <!-- /.register-box -->
    <!-- jQuery 2.1.4 -->
    <script src='@Url.Content("~/plugins/jQuery/jQuery-2.1.4.min.js")'></script>
    <!-- Bootstrap 3.3.5 -->
    <script src='@Url.Content("~/bootstrap/js/bootstrap.min.js")'></script>
    <!-- iCheck -->
    <script src='@Url.Content("~/plugins/iCheck/icheck.min.js")'></script>

    <script src='@Url.Content("~/Scripts/Constants.js")'></script>
    <script src='@Url.Content("~/Scripts/SatyaValidator.js")'></script>
    <script>
        $(function () {
            $('input').iCheck({
                checkboxClass: 'icheckbox_square-blue',
                radioClass: 'iradio_square-blue',
                increaseArea: '20%' // optional
            });

            $('#btnRegister').click(function () {

                var errMsg = $('#ErMsg');

                if (!RequiredFieldTextBox("Name", "ErName", "Enter name!")) {
                    $('#Name').parent().removeClass("has-feedback").addClass("has-error");
                    return false;
                } else {
                    $('#Name').parent().removeClass("has-error").addClass("has-feedback");
                }

                if (!RequiredFieldTextBox("Email", "ErEmail", "Enter email!")) {
                    $('#Email').parent().removeClass("has-feedback").addClass("has-error");
                    return false;
                } else {
                    $('#Email').parent().removeClass("has-error").addClass("has-feedback");
                }

                if (!EmailValidation("Email", "ErEmail", "Enter valid email!")) {
                    $('#Email').parent().removeClass("has-feedback").addClass("has-error");
                    return false;
                } else {
                    $('#Email').parent().removeClass("has-error").addClass("has-feedback");
                }

                if (!RequiredFieldTextBox("Password", "ErPassword", "Enter password!")) {
                    $('#Password').parent().removeClass("has-feedback").addClass("has-error");
                    return false;
                } else {
                    $('#Password').parent().removeClass("has-error").addClass("has-feedback");
                }

                if (!RequiredFieldTextBox("RePassword", "ErRePassword", "Re-enter password!")) {
                    $('#RePassword').parent().removeClass("has-feedback").addClass("has-error");
                    return false;
                } else {
                    $('#RePassword').parent().removeClass("has-error").addClass("has-feedback");
                }

                if (!CompareValidator("Password", "RePassword", "ErRePassword", "Password didi not match!")) {
                    $('#RePassword').parent().removeClass("has-feedback").addClass("has-error");
                    return false;
                } else {
                    $('#RePassword').parent().removeClass("has-error").addClass("has-feedback");
                }


                var data = {"name": $('#Name').val(), "email": $('#Email').val(), "password": $('#Password').val() };

                errMsg.html("Please wait...");
                errMsg.css("color", "#3057b2");
                errMsg.fadeIn(500);

                // Post data to server
                $.ajax({
                    type: "POST",
                    url: "/Account/Register",
                    async: true,
                    data: data,
                    success: function (response) {
                        if (response == OperationResponse.SERVER_ERROR) {
                            errMsg.html(OperationMessage.SERVER_ERROR);
                            errMsg.css("color", "#C53131");
                            errMsg.fadeIn(500);
                        }
                        else if (response == OperationResponse.SUCCESS) {
                            errMsg.html(OperationMessage.LOGIN_SUCCESS);
                            errMsg.css("color", "#308347");
                            errMsg.fadeIn(2000);

                            // After success full login redirect page to main site page
                            window.location.href = '@Url.Content("~/Account/Login")';
                        }
                        else if (response == OperationResponse.NOT_MATCH) {
                            errMsg.html(OperationMessage.PASSWORD_NOT_MATCH);
                            errMsg.css("color", "#C53131");
                            errMsg.fadeIn(500);

                        }
                        else if (response == OperationResponse.NOT_EXISTS) {
                            errMsg.html(OperationMessage.EMAIL_NOT_EXIST);
                            errMsg.css("color", "#C53131");
                            errMsg.fadeIn(500);
                        }
                    },
                    failure: function (response) {
                        errMsg.html(OperationMessage.SERVER_ERROR);
                        errMsg.css("color", "#C53131");
                        errMsg.fadeIn(500);
                    }
                });

                return false;
            });
        });
    </script>
</body>
</html>
